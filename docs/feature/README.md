# Historical Feature Exploration

This directory contains exploratory design artifacts from earlier phases. Keep
them for provenance, but use the root README and `docs/architecture/` documents
for current Tree Ring Memory behavior.

Earlier files may mention Python reference implementations or binding surfaces.
Those decisions have been superseded by the Rust-native runtime and CLI.
